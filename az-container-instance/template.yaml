properties:
  managedEnvironmentId: /subscriptions/${{ vars.ARM_SUBSCRIPTION_ID }}/resourceGroups/EIS_DEVTEST-INT-EAST-SANDBOX-RG/providers/Microsoft.App/managedEnvironments/CONTAINER_APP_ENVIRONMENT
  configuration:
    ingress:
      external: true
      allowInsecure: false
      targetPort: 80
  template:
    containers:
      - image: docker-documentconversion:${{ github.sha }}
        name: test-container-app